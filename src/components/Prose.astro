<div class="prose">
  <slot />
</div>

<style>
  .prose {
    --prose-strong: color-mix(in srgb, var(--accent-secondary) 82%, #f8efe0 18%);
    --prose-link: color-mix(in srgb, #9fd6ff 68%, var(--accent-tertiary) 32%);
    --prose-link-hover: color-mix(in srgb, #cce8ff 74%, var(--accent-tertiary) 26%);
    --prose-link-underline: rgba(142, 192, 232, 0.46);
    --prose-link-underline-hover: rgba(191, 228, 255, 0.74);
    width: min(100%, var(--content-width));
    margin: 0 auto;
    color: var(--text-primary);
    font-size: 1.08rem;
    line-height: 1.85;
  }

  .prose > :first-child {
    margin-top: 0;
  }

  .prose h2,
  .prose h3,
  .prose h4 {
    margin-top: 2.6rem;
    margin-bottom: 1rem;
    font-family: var(--font-sans-en-display);
    line-height: 1.15;
    letter-spacing: -0.03em;
  }

  .prose h2 {
    font-size: 2rem;
  }

  .prose h3 {
    font-size: 1.5rem;
  }

  .prose p,
  .prose ul,
  .prose ol {
    margin: 1.1rem 0;
  }

  .prose :global(a) {
    color: var(--prose-link);
    font-weight: 520;
    text-decoration: underline;
    text-decoration-color: var(--prose-link-underline);
    text-decoration-thickness: 0.09em;
    text-underline-offset: 0.2em;
    transition:
      color 180ms ease,
      text-decoration-color 180ms ease,
      text-shadow 180ms ease;
  }

  .prose :global(a:hover) {
    color: var(--prose-link-hover);
    text-decoration-color: var(--prose-link-underline-hover);
    text-shadow: 0 0 18px rgba(134, 194, 242, 0.18);
  }

  .prose :global(a:focus-visible) {
    outline: 1px solid rgba(166, 214, 249, 0.52);
    outline-offset: 0.22rem;
    border-radius: 0.2rem;
    color: var(--prose-link-hover);
    text-decoration-color: var(--prose-link-underline-hover);
  }

  .prose :global(strong) {
    color: var(--prose-strong);
    font-weight: 760;
    letter-spacing: 0.01em;
    text-shadow: 0 0 14px rgba(216, 191, 139, 0.12);
  }

  .prose :global(strong a),
  .prose :global(a strong) {
    color: inherit;
  }

  .prose figcaption {
    margin-top: 0.8rem;
    color: var(--text-secondary);
    font-size: 0.94rem;
    text-align: center;
  }

  .prose :global(code):not(:global(pre code)) {
    border: 1px solid rgba(216, 191, 139, 0.16);
    border-radius: 0.45rem;
    padding: 0.2rem 0.4rem;
    background: rgba(216, 191, 139, 0.08);
    color: color-mix(in srgb, var(--accent-secondary) 62%, #f5efe2 38%);
    font-size: 0.92em;
  }

  @media (max-width: 720px) {
    .prose {
      font-size: 1rem;
    }

    .prose :global(a) {
      text-decoration-thickness: 0.08em;
      text-underline-offset: 0.18em;
    }
  }
</style>
