<div class="prose">
  <slot />
</div>

<style>
  .prose {
    width: min(100%, var(--content-width));
    margin: 0 auto;
    color: var(--text-primary);
    font-size: 1.08rem;
    line-height: 1.85;
  }

  .prose > :first-child {
    margin-top: 0;
  }

  .prose h2,
  .prose h3,
  .prose h4 {
    margin-top: 2.6rem;
    margin-bottom: 1rem;
    font-family: var(--font-sans-en-display);
    line-height: 1.15;
    letter-spacing: -0.03em;
  }

  .prose h2 {
    font-size: 2rem;
  }

  .prose h3 {
    font-size: 1.5rem;
  }

  .prose p,
  .prose ul,
  .prose ol {
    margin: 1.1rem 0;
  }

  .prose a {
    color: var(--accent-secondary);
    text-decoration: underline;
    text-decoration-color: rgba(216, 191, 139, 0.38);
    text-underline-offset: 0.18em;
  }

  .prose strong {
    color: color-mix(in srgb, var(--accent-secondary) 72%, #fff 28%);
    font-weight: 700;
  }

  .prose figcaption {
    margin-top: 0.8rem;
    color: var(--text-secondary);
    font-size: 0.94rem;
    text-align: center;
  }

  .prose :global(code):not(:global(pre code)) {
    border: 1px solid rgba(216, 191, 139, 0.16);
    border-radius: 0.45rem;
    padding: 0.2rem 0.4rem;
    background: rgba(216, 191, 139, 0.08);
    color: color-mix(in srgb, var(--accent-secondary) 62%, #f5efe2 38%);
    font-size: 0.92em;
  }

  @media (max-width: 720px) {
    .prose {
      font-size: 1rem;
    }
  }
</style>
